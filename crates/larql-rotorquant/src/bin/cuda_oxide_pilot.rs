fn main() {
    // Intentionally empty. The binary target gives `cargo oxide build`
    // a manifest-level artifact name (`larql_rotorquant.ptx`) for the
    // kernels defined in the library crate.
}
