# Task: max-of-two

Implement `max_of_two(a: int, b: int) -> int` in `solution_scaffold/solution.py` so it returns
the larger of `a` and `b` (ties return either, since `a == b`).

Run `./test.sh` — exits 0 on success, non-zero on failure.
