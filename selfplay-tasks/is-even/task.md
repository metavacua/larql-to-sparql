# Task: is-even

Implement `is_even(n: int) -> bool` in `solution_scaffold/solution.py` so it returns `True` iff
`n` is even (`0` counts as even; negative numbers follow the usual definition).

Run `./test.sh` — exits 0 on success, non-zero on failure.
